import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const MedicineStock = () => {
    return (
        <View>
            <Text>MedicineStock</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
