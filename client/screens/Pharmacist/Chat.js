import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const Chat = () => {
    return (
        <View>
            <Text>Chat</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
